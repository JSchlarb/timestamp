pluginManagement {
  repositories {
    mavenLocal()
    gradlePluginPortal()
  }

  plugins {
    id 'org.springframework.boot' version "${springBootVersion}"
  }
}

include("producer")

rootProject.name = 'timestamp'
